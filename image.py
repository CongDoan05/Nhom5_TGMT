
# cv.line(img, (0, 0), (N-1, M-1), 255)

# sử dụng cv vẽ 1 đường tròn có tâm trùng với tâm của ảnh, có bán kính là 100, màu trắng ,độ dày 2 pixel




# plt.imshow(img, cmap='gray')
# plt.show()

# cv.imshow('image', img)
# if cv.waitKey(0) == ord('q'):
#     cv.destroyAllWindows()

# cv.line(img, (0, 0), (N-1, M-1), 255)

# m = 800
# n = 800
# c = 3

# cl_img = np.zeros((m, n, 3), dtype=np.uint8)
# # cl_img[:,:,1] = 255 # kênh màu xanh lá
# cl_img[:,:,2] = 255 # kênh màu đỏ
# # cl_img[:,:,0] = 255 # kênh màu xanh dương

# chessboard = np.zeros((800, 800, 3), dtype=np.uint8)
# for i in range(0, 800, 100):
#     for j in range(0, 800, 100):
#         if (i // 100) % 2 == (j // 100) % 2:
#             chessboard[i:i+100, j:j+100] = (255, 255, 255)
# cv.imshow('color image', chessboard)
# cv.waitKey(0)
# cv.destroyAllWindows()




    

    


